import React from 'react';
import './index.less';
import {Container} from 'taro-easy-ui';

export default class Index extends React.Component{
    render() {
        return (
            <Container/>
        );
    }
}