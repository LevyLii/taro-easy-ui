import {Common} from "./common";
import {DrawerProps} from "../../../types/drawer";

export default class DrawerTypes extends Common {
    constructor(props: DrawerProps) {
        super(
            props,
            {}
        )
    }
}