import {Common} from "./common";
import {TabsProps} from "../../../types/tabs";

export default class TabsTypes extends Common {
    constructor(props: TabsProps) {
        super(
            props,
            {}
        )
    }
}